const test=require('node:test'),assert=require('node:assert/strict'),h=require('./history-test-helper');let saved,s;
test.before(()=>{saved=h.backup();s=h.scope('rebuild');h.setup(s)});test.after(()=>h.restore(saved));
test('rebuilds deterministic states at each cursor',()=>{for(const width of[71,72])h.history.transact(s,{scope:'STRUCTURE',operation:'resizeColumn',target:{columnIndex:2},before:{},after:{width}},()=>h.structure.upsert({...s,logicalTableKey:'tbl:financial-7col',operation:'resizeColumn',columnIndex:2,afterWidth:width}));assert.equal(h.history.rebuildEditorStateFromHistory(s,0).structureOperations.length,0);assert.equal(h.history.rebuildEditorStateFromHistory(s,1).structureOperations[0].afterWidth,71);assert.equal(h.history.rebuildEditorStateFromHistory(s,2).structureOperations[0].afterWidth,72)});
