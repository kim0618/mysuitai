# Import MVP 2.0 Image DIM Design

The retry detected the report page at screenshot coordinates `(243, 58, 794, 1123)`. Because this exactly matches the template's 794 × 1123 DIM page, normalization used scale 1 on both axes and nearest-integer rounding.

The frozen first pass contains three TEXT elements and one 3 × 4 TABLE. Cells remain static strings. The observed red table background and white cell text are logged as unsupported because DIM v1.1 has no safe table style fields. The Import Generator received only the validated Freeze DIM and retained responsibility for UBJF cloning and serialization.
